// var angularServer = 'localhost:4200'
// var apiServer = '127.0.0.1:7091'

var angularServer = 'api.tudominio'
var apiServer = 'api.tudominio'


export const environment = {
  production: true,
  backend: `http://${angularServer}`, // Put your backend here
  api: `http://${apiServer}`};
